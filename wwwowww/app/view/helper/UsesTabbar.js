Ext.define( 'WWWOWWW.view.helper.UsesTabbar', {

    /**
     *  returns whether this component needs a tabbar.
     * @returns {boolean}
     */
    usesTabbar: function () {
        var usesTabbar = this.getUsesTabbar();
        return usesTabbar;
    }

} );